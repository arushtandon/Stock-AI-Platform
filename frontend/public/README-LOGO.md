# Safron logo

Replace `safron-logo.svg` with your company logo (SVG or PNG). Keep the same filename so the app picks it up, or update all `src="/safron-logo.svg"` in the app to your file name.

Recommended size: about 120×40px (or proportional) for the nav bar.
